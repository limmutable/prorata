# Sanity CMS Configuration
# Get these values from https://sanity.io/manage after creating your project

# IMPORTANT: Create a .env file (not .env.local) for Sanity Studio to work properly
# Copy this file to .env and fill in your actual values

# Required: Your Sanity project ID
NEXT_PUBLIC_SANITY_PROJECT_ID=your_project_id_here

# Optional: Dataset name (defaults to 'production')
NEXT_PUBLIC_SANITY_DATASET=production

# Optional: API token for write operations (only needed for Studio)
# SANITY_API_TOKEN=your_api_token_here

# Example values:
# NEXT_PUBLIC_SANITY_PROJECT_ID=abc123de
# NEXT_PUBLIC_SANITY_DATASET=production